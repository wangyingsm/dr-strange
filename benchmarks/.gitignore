# Generated dataset and scratch databases — regenerate with `just bench-gen`.
data/
*.redb
# Results are NOT ignored: benchmarks/results/*.json is the evidence behind
# BENCHMARKS.md (per-op figures, pass count, recorded spread) and is committed
# with every re-baseline so the published table can be checked and reproduced.
__pycache__/
