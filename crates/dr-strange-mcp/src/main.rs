//! drsg-mcp — MCP (JSON-RPC 2.0 over stdio) server embedding dr-strange-core
//! (arch/06). No database logic here.
//!
//! TODO(M4): tool surface — list_planes, describe_plane, get_node/edge,
//! search, traverse, query, write_nodes/edges, digest, plane lifecycle.

fn main() {
    eprintln!("drsg-mcp: not yet implemented (M4) — see arch/06-mcp.md");
    std::process::exit(2);
}
