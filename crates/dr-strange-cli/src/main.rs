//! drsg — CLI wrapper over dr-strange-core (arch/05). No database logic here.
//!
//! TODO(M4): real argument parsing (clap) and subcommands: init, plane,
//! import, digest, export, query, get, catalog, index, stats, check, bench.

fn main() {
    eprintln!("drsg: not yet implemented (M4) — see arch/05-tools.md");
    std::process::exit(2);
}
