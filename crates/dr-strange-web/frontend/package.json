{
  "name": "dr-strange-web-frontend",
  "private": true,
  "type": "module",
  "description": "dr-strange dashboard SPA (arch/08). Built with bun + Vite + Svelte; output in dist/ is embedded into the drsg binary.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "bun test",
    "preview": "vite preview",
    "lint": "oxlint -A all -D no-undef --config .oxlintrc.json src/"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.1.0",
    "oxlint": "^1.77.0",
    "svelte": "^5.19.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@sigma/edge-curve": "^3.1.0",
    "graphology": "^0.26.0",
    "graphology-layout-forceatlas2": "^0.10.1",
    "sigma": "^3.0.3"
  }
}
