//! Public API layer (arch/04): `Database`, `PlaneHandle`, write transactions,
//! and the query builder. The only surface wrappers may use.
//!
//! TODO(M0): `Database::open` / `in_memory`, plane lifecycle, and the
//! create-node → get-node → 1-hop-expand vertical slice.
//! TODO(M2): query builder mirroring plan operators one-to-one.

/// Placeholder — implementation lands in M0.
pub struct Database {}
