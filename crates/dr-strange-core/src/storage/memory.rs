//! In-memory `StorageEngine` backend (arch/01 §1, §7): fast tests and the
//! oracle for property-based testing against the redb backend.
//!
//! TODO(M0): implement `StorageEngine` for `MemoryEngine` (BTreeMap tables,
//! snapshot-on-read is sufficient at this stage).

/// Placeholder — implementation lands in M0.
#[derive(Debug, Default)]
pub struct MemoryEngine {}
